---
name: chun-memory
description: 会话记忆与上下文管理。当用户说「记录」「存档」「记住」「开始记录」时，把当前对话的全量原文（含图片/代码/HTML 等生成物）分段存档到 D:\Mark；当用户说「回忆」时，从已存档内容精准检索并回答。跨会话、跨 agent、任何 AI 都能用。Use whenever the user says 记录/存档/记住/回忆, or wants to persist or recall earlier conversation details.
---

# chun-memory

把「当前对话」持久化成 `D:\Mark` 下的一套普通 MD 档案，让后面的对话（同一个或另一个 AI）能精准回忆起前面的具体内容——包括当时的原文和生成出来的文件。不依赖任何平台内置记忆，只靠普通文件和路径，因此任何 AI 都能读写。

## 目录结构

```
D:\Mark\
├─ index.md                          # 顶层索引（每次存档后更新）
└─ <时间>-<主题>\                     # 每次对话一个独立文件夹，例：2026-09-06-1830-记忆skill设计
     ├─ index.md                     # 本会话索引（每次存档后更新）
     ├─ 001.md                       # 第 1 段原文（约 ≤150k token）
     ├─ 001-assets\                  # 第 1 段涉及的生成物（图片/代码/HTML 等）
     ├─ 002.md                       # 第 2 段原文（增量，只含上次存档之后的新内容）
     └─ 002-assets\
```

- 文件夹名 = `YYYY-MM-DD-HHMM-<主题>`，主题用简短中文短语概括本会话在做什么。
- 分段文件 `001.md`、`002.md`… 是**非重叠增量**：`001.md` 只装第 1 段，`002.md` 只装第 1 段之后的新内容，绝不重复写已经存过的部分。

## 存档（触发词：记录 / 存档 / 记住）

无论用户说「记录」「存档」「记住」，都执行下面**同一套动作**：

1. **定位本次要写哪一段**：先看 `D:\Mark\index.md` 和当前会话文件夹里的 `index.md`。
   - 本会话第一次存档 → 新建文件夹 `<时间>-<主题>`，写 `001.md`。
   - 之后再次存档 → 在已有文件夹里写 `002.md`、`003.md`…（只写「上次存档之后到现在」的新对话）。

2. **写原文**：把「上次存档之后到当前时刻」的对话内容**忠实、完整**地写成 Markdown。
   - 按时间顺序，尽量逐字保留用户和助手各自说了什么；生成物用占位标注（见第 3 步）。
   - **分块多次写入**：不要试图一次写完一大段，而是按轮次切成若干批，分批 `write`/`edit` 追加到同一个分段文件里。一次写太多容易中途截断、丢内容；分批写才能把原文复述得最完整、最接近原话。
   - 分段文件**头部**写一行元信息：
     ```
     <!-- 主题：… | 日期：… | 范围：本段覆盖的对话起止 | 摘要：… | 关键词：a, b, c -->
     ```
     摘要是给「人/AI 一眼看懂这段讲了什么」用的，关键词是给 grep 用的。

3. **归档生成物**：把本段对话里「生成出来的文件」（图片、代码文件、HTML、文档等）**复制**进对应的 `XXX-assets\` 文件夹（`XXX` 与本段文件名一致）。
   - 在分段 MD 里对应位置写清楚 `→ 见 assets\xxx.png`，并**说明这个文件是什么、有什么用**。
   - 图片没法塞进 Markdown 正文，所以必须复制原文件 + 在 MD 里记相对路径，否则以后取不回来。

4. **更新两级 index**（每次存档后都必须做）：
   - 本会话 `index.md`：追加本段信息——摘要、关键词、分段文件清单、产物清单与路径。
   - 顶层 `D:\Mark\index.md`：追加本会话的摘要、关键词、文件夹路径。
   - 两个 index 的**头部都固定写一段「使用说明」**（见下），让任何 AI 只读 index 就能学会怎么检索。

## 召回（触发词：回忆）

用户说「回忆 X」或想找回以前的内容时：

1. 先读 `D:\Mark\index.md`，按关键词/主题找到相关会话文件夹。
2. 再读该文件夹里的 `index.md`，定位到具体分段文件。
3. 打开对应分段，或 `grep` 关键词精确定位，用原文回答用户，并说明出处（哪个文件哪一段）。
4. 需要生成物时，按 MD 里记的相对路径把文件找回来。

检索原则是「**先 index 后原文**」：150k 的原文不能整段重读（会又占满上下文），所以 index 是第一入口，原文只在定位到具体分段后再读。

## index 的「使用说明」（跨 agent 关键）

每个 index.md 顶部放这一段，任何 AI 读到它就能照做：

```
## 使用说明
本目录是 chun-memory 的对话记忆档案。
- 检索方式：先读本 index 定位，再打开对应分段原文（001.md / 002.md …）。
- 分段文件头部有「主题/日期/范围/摘要/关键词」，摘要用于快速判断，关键词用于 grep。
- 生成物存放在与分段同名的 -assets 文件夹，路径在分段原文里注明。
```

## 阈值与触发说明

- 分段大小目标 ≈ 150k token，但 agent 无法精确数 token，所以**以估算为主**（按累计轮数 × 平均长度粗估），用户说「记录/存档/记住」时强制切新段兜底。
- 存档动作完全由用户手动触发；没有真正的后台自动触发。
